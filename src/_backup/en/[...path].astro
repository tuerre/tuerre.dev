<!-- ---
// Catch-all: el sitio es 100% español-only (sin rutas /en/*). Cualquier
// subruta bajo /en redirige siempre a la home con un 301 permanente.
// Renderizado bajo demanda (no prerender) porque el destino cubre rutas
// arbitrarias que no se conocen en build time.
export const prerender = false;

return Astro.redirect("/", 301);
--- -->
