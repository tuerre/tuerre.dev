<!-- ---
// Catch-all: el sitio es 100% español-only (sin rutas /es/*). Cualquier
// subruta bajo /es redirige siempre a la home con un 301 permanente.
// Renderizado bajo demanda (no prerender) porque el destino cubre rutas
// arbitrarias que no se conocen en build time.
export const prerender = false;

return Astro.redirect("/", 301);
--- -->
